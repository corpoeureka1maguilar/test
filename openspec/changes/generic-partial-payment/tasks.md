# Tasks: Gift Card + N Cobros VPOS Sucesivos (generic-partial-payment)

Backend: openspec (file-based). Strict TDD is ACTIVE. Test runner: `npm test` (Vitest).
Every implementation task is preceded by its RED (failing test) task — do not write
implementation before the test fails for the right reason (red → confirm → green).

Legend: `[P]` = can run in parallel with siblings in the same phase. `[S]` = sequential,
hard dependency on a prior task in this file.

---

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1150-1250 (impl + tests) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 → PR4 → PR5 |
| Delivery strategy | ask-on-risk (default — not specified by caller) |
| Chain strategy | pending — user must choose |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | `types.ts` + `paymentMethodRepository.ts` + `paymentConfig.ts` + `saleMachine.ts` (`legs[]`, `VPOS_LEG_PAID`, `commitLeg`, `coversRemaining`) | PR1 | ~325 lines. Foundational, highest regression risk (touches actor input contracts). Base: main/tracker. |
| 2 | `saleOrderPayload.ts` + `printPayload.ts` (`tenders[]`, real `printerCode`, IGTF per leg) | PR2 | ~330 lines. Pure builders, no React. Depends on PR1's `PaymentLeg` type + `legs` context shape. |
| 3 | `useVposCheckout.ts` generalization (`VPOS_LEG_PAID` dispatch, confirmed-amount param) | PR3 | ~135 lines. Depends on PR1 (event/guard must exist). |
| 4 | `VposAmountInput.tsx` (new) + `PaymentForm.tsx` wiring + `PaymentSelect.tsx` (re-select, cap, printerCode filter, legs render) | PR4 | ~285 lines. Depends on PR1-PR3. |
| 5 | Retry/resiliencia integration test + full regression pass + hardware gate | PR5 | ~120 lines + manual checklist. Depends on PR1-PR4. |

**Work Unit 1 (PR1) — DONE (2026-07-24)**: Phase 0 (0.1/0.2/0.3, decisions-only) + Phase 1 (1.1-1.5) implemented and merged into the working tree (uncommitted — single commit for the whole change happens after all work units, per user instruction). Files touched: `src/shared/types/types.ts`, `src/shared/lib/odoo/paymentMethodRepository.ts` (+ new `.test.ts`), `src/shared/lib/paymentConfig.ts` (new), `src/features/payment/machines/saleMachine.ts` (+ `.test.ts`), plus a regression fix in `src/shared/lib/odooRepository.test.ts` (facade test for the same `fetchPaymentMethods` function). Full suite: 43 files / 365 tests green; `tsc --noEmit` clean. PR2-PR5 remain for subsequent work units.

**Work Unit 2 (PR2) — DONE (2026-07-24)**: Phase 2 (2.1-2.4) implemented. Files touched: `src/shared/lib/saleOrderPayload.ts` (+ `.test.ts`, full rewrite to `legs[]` signature), `src/shared/lib/printPayload.ts` (+ `.test.ts`, `buildFacturaPayload` now takes `tenders[]`; `computeGiftCardSplitTender` removed, superseded by caller-side tender construction), `src/features/payment/machines/saleMachine.ts` (call sites: `submitPaymentToOdoo`/`enqueueOfflineOrder` now pass `legs` to `buildSaleOrderPayload`; `printFiscalInvoice` builds `tenders[]` from `legs`+`giftCard` and calls the new `buildFacturaPayload`; `buildLegsInput` rewritten — see deviations below), plus a 1-line fixture fix in `src/features/payment/machines/saleMachine.test.ts` (added `printerCode: '01'` to the top-level `method` fixture — see Deviation #2). Full suite: 43 files / 368 tests green; `tsc --noEmit` clean.
- **Deviation #1 (design-table wording vs. literal task wording, resolved per task/spec — the more specific instruction)**: design.md's Interfaces section says `montoIgtf:l.montoIgtf` (trust the leg's own precomputed field) for `saleOrderPayload.ts`'s `payments.map`, but task 2.2's own prose and the spec's "IGTF Calculated Per Leg" requirement (#9) are explicit: `montoIgtf: calcIgtf(l.method, l.baseBs)`, computed fresh by `buildSaleOrderPayload` itself, never trusted from elsewhere. Implemented the literal task/spec version — `buildSaleOrderPayload` now recomputes `calcIgtf(leg.method, leg.baseBs)` independently instead of reading `leg.montoIgtf`. In practice the two are numerically equivalent for every leg source in this codebase (`commitLeg` already sets `leg.montoIgtf` via `calcIgtf`), so this is a defense-in-depth/spec-literalness choice, not a behavior change.
- **Deviation #2 (real regression found and fixed, same pattern as Work Unit 1's task 1.2)**: `buildFacturaPayload` now throws when a tender's `code` is empty (fiscal-tender-code-mapping requirement). `saleMachine.test.ts`'s single real (non-mocked) `printFiscalInvoice` exercise ("printing wiring: the REAL printFiscalInvoice...") used a `method` fixture with no `printerCode` — added `printerCode: '01'` to that fixture (1-line, non-assertion change) so the throw doesn't fire for a fixture that predates the field's existence. Also corrected a now-stale comment in the same test referencing the removed `computeGiftCardSplitTender`.
- **Deviation #3 (design gap closed, not in the literal RED-test list, required to keep 2 pre-existing real-actor-body tests green — `saleMachine.test.ts` T4.1 "offline replay integration" and "printing wiring")**: neither design.md nor tasks.md's 2.1/2.3 RED lists specify how the LEGACY (non-VPOS, `SUBMIT_PAYMENT`) single-leg synthesis in `buildLegsInput` (built in Work Unit 1/1.5, but dead code until this work unit wired actor bodies to consume it) should compute `baseBs`/`amountBs`. Discovered that the PRE-Phase-2 `saleOrderPayload.ts`/`printPayload.ts` NEVER read `ActivePayment.amount`/`.igtfAmount` for the payment/tender amount at all (only `.reference`) — they always recomputed the total from `cart`+`method` internally (in Bs), specifically because `ActivePayment.amount` can be in a FOREIGN currency (USD) for methods with `currencyRate > 1` (see `usePaymentAmounts.ts`), while `payments[].amount`/fiscal tenders must always be in Bs. If `buildLegsInput` had instead read `activePayment.amount` directly (as Work Unit 1's version did, since it was unused at the time), foreign-currency legacy payments would have silently sent a USD number as if it were Bs, and the legacy gift-card-remainder byte-identical requirement (0.1) would have broken (confirmed against `saleMachine.test.ts`'s 2 real-body tests, which use different historical cart-total formulas — WITH tax for the Odoo/offline path, WITHOUT tax for the print path — that both had to keep producing their exact pre-Phase-2 numbers). **Resolution**: `buildLegsInput` now takes a `totalBsForRemainder` param supplied by each of the 3 state `input:` functions (`processing`/`enqueuingOffline` pass the with-tax cart total matching `saleOrderPayload.ts`'s old formula; `printing` passes the no-tax cart total matching `printPayload.ts`'s old formula) and recomputes `baseBs`/`montoIgtf`/`amountBs` from `cart`+`selectedMethod` (and, when a legacy gift-card remainder applies, subtracts the gift card's consumed Bs and forces `montoIgtf: 0`, exactly as the pre-Phase-2 code did) — never from `activePayment.amount`/`.igtfAmount` directly. This is a correctness fix for a latent gap in Work Unit 1's scaffold, not a new design decision; it was necessary precisely because Phase 2 is the first work unit where that scaffold's output is actually consumed.
- **Deviation #4 (scope boundary, not a functional change)**: `buildNotaCreditoPayload` (credit-note refunds, `useOrderReturn.ts`) keeps its historical hardcoded tender code (`'01'` normal / `GIFT_CARD_TENDER_CODE` for `-999`) instead of deriving from `method.printerCode` — its `NO_IGTF_METHOD` fixture deliberately never carries the original order's real payment method (see its own code comment), so no real `printerCode` is available at refund time. The "never default a tender code" rule (fiscal-tender-code-mapping) is scoped to piernas of an in-progress sale's payment split, not to refunds; forcing it onto `buildNotaCreditoPayload` would have broken production credit-note printing with no spec/design mandate to do so. Documented as a code comment at the call site.

**Work Unit 3 (PR3) — DONE (2026-07-24)**: Phase 3 tasks 3.1-3.2 only (per explicit run scope — 3.3-3.6 and Phase 4 deferred to later work units). Files touched: `src/features/payment/hooks/useVposCheckout.ts` (new `confirmedBaseBs` param; `'00'` now dispatches `VPOS_LEG_PAID` instead of `SUBMIT_PAYMENT`; navigation decided by `confirmedBaseBs >= (context.remainingAmount ?? totalWithIgtfBs)`), `src/features/payment/hooks/useVposCheckout.test.ts` (new, 5 tests), `src/features/payment/pages/PaymentForm.tsx` (1-line call-site wiring: `confirmedBaseBs: effectiveTotal` temporary default + `TODO` comment, no other UI change). Full suite: 44 files / 373 tests green; `bun run typecheck` clean. See 3.1/3.2 Result notes above for the one flagged (expected, pre-announced) deviation: the temporary `confirmedBaseBs` default at the `PaymentForm.tsx` call site, pending `VposAmountInput` (tasks 3.3/3.4, next work unit).

**Work Unit 4 (PR4) — DONE (2026-07-24)**: Phase 3 tasks 3.3-3.6 (per explicit run scope — Phase 4 deferred to a later work unit). Files touched: `src/features/payment/components/VposAmountInput.tsx` (new) + `.test.tsx` (new, 9 tests), `src/features/payment/hooks/useVposCheckout.ts` (new optional `confirmed` gate param, default `true`, backward-compatible), `src/features/payment/hooks/useVposCheckout.test.ts` (+3 tests for the gate), `src/features/payment/pages/PaymentForm.tsx` (replaced the Work Unit 3 temporary `confirmedBaseBs: effectiveTotal`/`TODO` with real `VposAmountInput` wiring — new `confirmedBaseBs` state, conditional render VposAmountInput vs. VposPaymentView, `amounts` computed off the confirmed base for VPOS methods), `src/features/payment/pages/PaymentForm.test.tsx` (+2 integration tests), `src/features/payment/pages/PaymentSelect.tsx` (printerCode filter, `MAX_PAYMENT_LEGS` cap, legs/remainder render, stale Scenario-4 comment updated), `src/features/payment/pages/PaymentSelect.test.tsx` (+9 tests across 4 new describe blocks), `src/features/payment/pages/PaymentSelect.module.css` (+`.legsSummary`/`.capMessage`). Full suite: 45 files / 396 tests green; `bun run typecheck`: clean. See 3.4's Result notes for the 2 flagged deviations (necessary `useVposCheckout.ts` gate change beyond the literal file list; scope note on `usePaymentAmounts.ts`) and 3.6's Result note for the 1 flagged deviation (pure exclusion vs. disabled+message for empty-`printerCode` methods).

**Post-Work-Unit-4 manual changes (by the user, verified green, not reverted)**: `useGiftCardPayment.ts`/`GiftCardPaymentView.tsx` gained a "Saldo a consumir ($)" input to customize the USD amount debited from the gift card (clamped to `Math.min(balance, orderTotalUSD)`), recalculating the Bs remainder live; `VposAmountInput.tsx` got a visual redesign (glassmorphic card showing order total + current pending balance + USD equivalent + projected remaining amount); numeric inputs in both components moved to `step="0.0001"`/`min={0.0001}` (4-decimal precision). Tests (`useGiftCardPayment.test.ts`, `GiftCardPaymentView.test.tsx`, `VposAmountInput.test.tsx`) were updated accordingly. Baseline re-confirmed at the start of Work Unit 5: `bun run test` 45 files / 401 tests green, `bun run typecheck` clean.

**Work Unit 5 (PR5) — DONE (2026-07-24)**: Phase 4 tasks 4.1-4.2 (4.3 is a manual hardware gate, left PENDING — not executable in this environment). See 4.1/4.2 Result notes below for full detail. Full suite unchanged at 45 files / 401 tests green; `bun run typecheck` clean (no production code was modified in this work unit — 4.1's test was found already written and green from a prior, undocumented pass at this task; 4.2 is a verification-only pass).

---

## Phase 0 — Decisions locked before coding (sequential, no code)

### 0.1 [S] Lock legacy single-leg wrapping decision — DONE (2026-07-24)
- **Gap resolved**: design.md changes `buildSaleOrderPayload`'s signature to `(customer, cart, legs, attemptId, giftCard)` but does not say how the LEGACY single-method path (non-VPOS test methods: cash/pago_movil/zelle/etc., and full-gift-card via `SUBMIT_PAYMENT`) feeds this signature.
- **Decision**: those paths keep dispatching `SUBMIT_PAYMENT` unchanged. The `processing`/`enqueuingOffline`/`printing` actor **input mappers** wrap `context.activePayment` + `context.selectedMethod` into a synthetic one-element `legs` array ONLY at the input-construction boundary — never written into `context.legs`, which stays "VPOS legs only" per design Decision 1. This keeps the legacy path byte-identical.
- Document this as a code comment in `machines/saleMachine.ts` before 1.5.
- **Result**: Documented as a code comment block at the top of `machines/saleMachine.ts` (right after the imports, before the `Context` interface), covering 0.1/0.2/0.3 together. Implemented `buildLegsInput(context)` helper: returns `context.legs` when non-empty, else synthesizes a one-element `PaymentLeg[]` from `activePayment`/`selectedMethod` — added as an extra `legs` field on the `processing`/`enqueuingOffline`/`printing` actor inputs (actor BODIES still consume the old singular `payment`/`method` fields unchanged — Phase 2 wires them to actually use `legs`). Zero behavior change for the legacy path (verified: all 25 pre-existing `saleMachine.test.ts` tests + full suite stayed green).

### 0.2 [S] Lock VPOS partial-amount input UX (post-design user decision) — DONE (2026-07-24, documented only, no code this batch)
- **Not in design.md as a concrete UI**: design's Decision 3 says the leg amount is "confirmado antes de lanzar el terminal" abstractly; File Changes row for `PaymentForm.tsx` only says "pasar `baseBs`/tope a la pierna VPOS" — no component is named.
- **Decision (explicit, from user, post-design)**: add a new input, pre-filled with the FULL remaining amount by default (`remainingAmount ?? total`), editable only DOWNWARD (max = remainder), never empty or free-form. Confirming without editing preserves today's behavior (full remainder, single closing VPOS leg).
- Feeds 3.3/3.4 below.
- **Result**: Decision recorded verbatim as a code comment in `machines/saleMachine.ts` (0.2 block). No `VposAmountInput.tsx`/`PaymentForm.tsx` code touched in this work unit — that's Phase 3 (tasks 3.3/3.4), out of scope for this PR.

### 0.3 [S] Buying-a-new-gift-card branch must still compile against the new signature — DONE (2026-07-24, documented; code change deferred to Phase 2)
- **Gap resolved**: design.md's File Changes table doesn't mention `submitPaymentToOdoo`'s `cart.find(item => item.isGiftCard)` branch (buying a gift card AS A PRODUCT — unrelated to `giftCardLeg`/paying-with-a-card). Since `buildSaleOrderPayload` moves to `legs[]`, this branch must synthesize its own one-element `legs` array from its existing `payment`/`method` locals, out of scope for VPOS-split/cap logic. Covered in 1.5.
- **Result**: Documented as a code comment in `machines/saleMachine.ts` (0.3 block) right above `submitPaymentToOdoo`'s `giftCardItem` branch. No functional change made here — `buildSaleOrderPayload`'s signature is still the pre-Phase-2 one (`payment`/`method` singular), so this branch continues to compile and behave unchanged. The actual `legs`-array synthesis for this branch is deferred to Phase 2 (task 2.2), when `buildSaleOrderPayload` itself migrates.

---

## Phase 1 — Foundational (parallel among sub-groups)

### 1.1 [P] RED: paymentMethodRepository — failing test for `printer_code` fetch/map — DONE (2026-07-24)
- **File**: `src/shared/lib/odoo/paymentMethodRepository.test.ts` (new)
- **Satisfies**: fiscal-tender-code-mapping / "Real printer_code Sourced Per Method"
- Assert `search_read` is called with `printer_code` in the `fields` list, and `mapMethod` maps `printer_code: '05'` → `printerCode: '05'`, `printer_code: false` → `printerCode: ''` (no silent default other than empty string).
- **Result**: Created the file with 3 tests (fields list assertion + the 2 mapping assertions). Confirmed RED via `bun test`: 3/3 failed for the right reason (`fields` array missing `printer_code`; `printerCode` `undefined` instead of `'05'`/`''`).

### 1.2 [S] GREEN: types.ts + paymentMethodRepository.ts — real `printer_code` + `PaymentLeg` — DONE (2026-07-24)
- **Depends on**: 1.1 (red)
- **Files**: `src/shared/types/types.ts`, `src/shared/lib/odoo/paymentMethodRepository.ts`
- Add `printerCode?: string` to `KioskPaymentMethod`. Add `PaymentLeg { method; amountBs; baseBs; montoIgtf; reference; ts }` (per design Interfaces/Contracts).
- `RawMethod.printer_code: string | false`; add `'printer_code'` to `search_read` fields; `mapMethod` → `printerCode: r.printer_code || ''`.
- Run `npm test` until green.
- **Result**: Implemented exactly as specified. `bun test src/shared/lib/odoo/paymentMethodRepository.test.ts`: 3/3 green.
  **Regression found and fixed**: `src/shared/lib/odooRepository.test.ts` has its OWN `fetchPaymentMethods` test block (facade re-export of the same function) — its 3 tests hardcoded the old `fields` array and raw fixtures without `printer_code`, so they broke the moment `printer_code` was added to the fields list. Updated those 3 tests (added `printer_code`/`printerCode` to fixtures, fields arrays, and expected mapped output) — `bun test src/shared/lib/odooRepository.test.ts`: 25/25 green again.

### 1.3 [P] Config constant — `MAX_PAYMENT_LEGS` — DONE (2026-07-24)
- **File**: `src/shared/lib/paymentConfig.ts` (new)
- `export const MAX_PAYMENT_LEGS = 4` (gift card counts as 1, per design Cap note). No test required — trivial constant, consumed by 3.6.
- Fully independent — safe to do anytime.
- **Result**: Created as specified, with a comment documenting the v1-constant/future-config-store note from design's Open Questions.

### 1.4 [P] RED: saleMachine — failing tests for `legs[]` / `VPOS_LEG_PAID` / `coversRemaining` / `commitLeg` — DONE (2026-07-24)
- **File**: `src/features/payment/machines/saleMachine.test.ts`
- **Satisfies**: generic-partial-payment / "N-Leg Payment Composition", "VPOS Leg Failure Recovery"
- Write failing tests asserting:
  1. `enteringDetails` --`VPOS_LEG_PAID`--> with `baseBs < remainingAmount` → target `selectingMethod`; `context.legs` grows by 1 entry; `context.remainingAmount` decrements by `baseBs`.
  2. Same event with `baseBs === remainingAmount` (`coversRemaining` true) → target `processing`; `context.legs` includes the closing entry.
  3. `GIFT_CARD_PARTIAL` behavior is UNCHANGED (regression) — still unconditional loop to `selectingMethod`, still sets `giftCardLeg`/`remainingAmount`, never touches `legs`.
  4. `saleAttemptId` stable across 2+ `VPOS_LEG_PAID` loop iterations.
  5. `resetContext` includes `legs: []`.
  6. Regression: `SUBMIT_PAYMENT` (legacy non-VPOS/full-gift-card path) still goes straight to `processing` unchanged, never touches `legs`.
- **Result**: Added new describe block "saleMachine — generic N-leg VPOS payment (VPOS_LEG_PAID / legs[] / coversRemaining / commitLeg)" with 7 tests (the 6 listed + 1 extra regression test — see Deviation note in 1.5). Confirmed RED: `bun test` failed on `vi.hoisted` incompatibility with this file (pre-existing, unrelated to these tests — Bun's built-in test runner doesn't implement `vi.hoisted`); fell back to `bun run test` (vitest), which ran correctly: 7/7 new tests failed for the right reason (`VPOS_LEG_PAID` unrecognized by the machine, `context.legs` undefined), 25/25 pre-existing tests stayed green.

### 1.5 [S] GREEN: saleMachine.ts — implement `legs[]`, `VPOS_LEG_PAID`, `commitLeg`, `coversRemaining` — DONE (2026-07-24)
- **Depends on**: 1.4 (red), 1.2 (`PaymentLeg` type), 0.1, 0.3 (decisions)
- **File**: `src/features/payment/machines/saleMachine.ts`
- Add `legs: PaymentLeg[]` to context + reset + initial context.
- Add `VPOS_LEG_PAID { payment; method; baseBs }` event, `commitLeg` action (push leg, decrement `remainingAmount`), `coversRemaining` guard.
- `enteringDetails.on.VPOS_LEG_PAID`: guarded array `[{ guard: 'coversRemaining', target: 'processing', actions: 'commitLeg' }, { target: 'selectingMethod', actions: 'commitLeg' }]`.
- Update `processing`/`enqueuingOffline`/`printing` input mappers: when `context.legs.length > 0`, build input from `legs` + `giftCardLeg`; else (legacy path, per 0.1) synthesize a one-element legs array from `activePayment`/`selectedMethod`.
- Adapt the buying-a-gift-card-as-product branch in `submitPaymentToOdoo` per 0.3.
- Run `npm test` until green.
- **Result**: Implemented as specified: `legs: PaymentLeg[]` added to context/reset/initial context; `VPOS_LEG_PAID` event added to `SaleEvent`; `commitLeg` action (uses `calcIgtf(method, baseBs)` for real per-leg IGTF, per spec "IGTF Calculated Per Leg" — not hardcoded, even though this batch doesn't wire it into the Odoo payload yet, that's Phase 2); `coversRemaining` guard; `enteringDetails.on.VPOS_LEG_PAID` guarded-array transition exactly as specified. `buildLegsInput(context)` helper added (0.1) and wired into all three actor inputs as an extra `legs` field. `bun test src/features/payment/machines/saleMachine.test.ts` initially failed on `vi.hoisted` (Bun-runner incompatibility, same as 1.4) → used `bun run test` (vitest) as the working command for this file/suite going forward. All 32 tests green (25 pre-existing + 7 new). Full suite (`bun run test`): 43 files / 365 tests, all green. `bun run typecheck`: clean.
  - **Deviation #1 (documented decision, not silent)**: the `processing`/`enqueuingOffline`/`printing` validation guards (`if (!context.customer || !context.activePayment || ...) throw`) previously REQUIRED `activePayment`+`selectedMethod` unconditionally. Since a sale can now close purely via `VPOS_LEG_PAID` (which never calls `setPayment`/`setMethod` — those are legacy-path-only), the guards were loosened to `customer && saleAttemptId && (activePayment&&selectedMethod || legs.length>0)`. `payment`/`method` are still passed to the actor input for the legacy path (cast through `as ActivePayment`/`as KioskPaymentMethod` when null, since the underlying actor bodies — unchanged in this batch — still consume them directly). **Known, explicitly flagged limitation**: if a REAL (non-stubbed) `submitPaymentToOdoo`/`enqueueOfflineOrder`/`printFiscalInvoice` actor is invoked for a sale closed by `VPOS_LEG_PAID` with NO legacy `activePayment`/`selectedMethod` at all (pure multi-VPOS-leg, no gift card, no `SUBMIT_PAYMENT`), the actor bodies would crash on `payment.reference`/`method.id` since `buildSaleOrderPayload` still expects singular `payment`/`method` — this is expected and intentional: Phase 2 (task 2.2) rewires those actor bodies to consume `legs` instead, closing this gap. No test in this batch exercises that exact combination (all new tests either stub the actors or reach `processing`/`printing` via the legacy gift-card-remainder path), so no regression was introduced — this is a forward-looking, documented gap for the next work unit, not a bug in this one.
  - **Deviation #2 (guard/action fallback for `remainingAmount === null`)**: neither design.md nor tasks.md's literal RED-test list specifies what `coversRemaining`/`commitLeg` should do when `context.remainingAmount` is `null` (i.e. the FIRST `VPOS_LEG_PAID` of a sale with NO prior gift-card leg and NO prior VPOS leg — spec scenario "2+ sequential VPOS legs, no gift card" needs this, but that scenario's own traceability row spans 1.4/1.5 + 2.1/2.2 + 3.1/3.2, so Phase 1 alone isn't expected to fully resolve it). **Decision made and documented as a code comment** in `saleMachine.ts` (next to both the guard and the action): treat `remainingAmount === null` as "this leg covers the full amount" (`coversRemaining` returns `true`; `commitLeg` decrements from the leg's own `baseBs`, landing at `remainingAmount: 0`). This exactly preserves today's regression requirement ("Single VPOS-only sale unaffected" — a lone VPOS charge with no gift card closes immediately) and is covered by new test #7. Phase 3 (`useVposCheckout.ts`, task 3.2, out of scope for this batch) is where the UI-level "how much is the full remainder when nothing has been paid yet" computation (`totalWithIgtfBs`) already lives (per `PaymentForm.tsx`'s existing `effectiveTotal = context.remainingAmount ?? total`) — if a future work unit needs the MACHINE's own guard to independently reconstruct that total from `context.cart` (e.g. for a stricter guard that doesn't rely on the null-fallback), that's an explicit decision for Phase 3/4, not silently made here.

---

## Phase 2 — Pure builders (depends on Phase 1: 1.2, 1.5)

### 2.1 [P] RED: saleOrderPayload — failing tests for `legs[]` signature + real IGTF — DONE (2026-07-24)
- **File**: `src/shared/lib/saleOrderPayload.test.ts`
- **Satisfies**: generic-partial-payment / "IGTF Calculated Per Leg"; gift-card-partial-payment / "Multi-Leg Payload"
- Assert new signature `(customer, cart, legs, attemptId, giftCard)`: `payments.length === legs.length`; each `payments[i].amount === legs[i].amountBs`; each `payments[i].montoIgtf === calcIgtf(legs[i].method, legs[i].baseBs)` (NOT hardcoded `0`); full gift card (`method.id === -999`) → `payments: []` unchanged; single-leg legacy payment (synthesized per 1.5) → payload unchanged from today (regression).
- **Documented conscious behavior change**: update the OLD test that asserted `montoIgtf` forced to `0` on the remainder leg — replace with an assertion on `calcIgtf(method, baseBs)`. Numeric result stays `0` today (no production method has `applyIgtf: true`), but the code path is no longer hardcoded. Flag this explicitly in the test description per requirement #9.
- **Result**: Full rewrite of the file (old signature no longer compiles). 15 tests total: base mapping/rate/lines/reference/transactions regressions, a dedicated per-leg-IGTF-via-calcIgtf test, an N-leg composition test (2 legs, distinct methods, independent IGTF per leg), and a gift-card group (partial remainder + 1 leg, the documented conscious `montoIgtf` change with an explicit IGTF-applying method, full-gift-card regression, normal-single-leg regression, partial + 2 legs summing to total, and a "never more legs than passed" guard). Confirmed RED against the OLD `saleOrderPayload.ts` (old positional signature `(customer,cart,payment,method,attemptId,giftCard)` doesn't accept a `legs` array — compile/shape mismatch) before writing 2.2's implementation.

### 2.2 [S] GREEN: saleOrderPayload.ts — implement `legs[]` + real per-leg IGTF — DONE (2026-07-24)
- **Depends on**: 2.1 (red)
- **File**: `src/shared/lib/saleOrderPayload.ts`
- Replace `isPartialRemainder`/forced-`montoIgtf:0` branch with `legs.map(l => ({ ..., amount: l.amountBs, montoIgtf: calcIgtf(l.method, l.baseBs), journal: l.method.journalId, method: l.method.id }))`. Keep `isFullGiftCard` branch (`payments: []`) untouched.
- Update all call sites in `saleMachine.ts` actors to pass `legs` instead of singular `payment`/`method`.
- Run `npm test` until green.
- **Result**: Implemented as specified — `tenderLegs = legs.filter(l => l.method.id !== -999)` replaces the old `isFullGiftCard` flag (filtering out the synthetic -999 leg reproduces `payments: []` without a separate branch); `payments = tenderLegs.map(...)` with `montoIgtf: calcIgtf(leg.method, leg.baseBs)` recomputed fresh (see Work Unit 2 Deviation #1 in the header note above re: design-table wording vs. literal task/spec wording). Updated `submitPaymentToOdoo`/`enqueueOfflineOrder` call sites in `saleMachine.ts` to pass `legs` (with the gift-card-as-product branch in `submitPaymentToOdoo` synthesizing its own one-element `legs` array per 0.3). Also had to rewrite `buildLegsInput` (see Work Unit 2 Deviation #3) to correctly recompute `baseBs`/`amountBs` from cart+method instead of trusting `activePayment.amount` — a latent gap in Work Unit 1's version that only surfaced once this task actually wired it into a consumer. `bun run test`: 15/15 new + 368/368 full suite green.

### 2.3 [P] RED: printPayload — failing tests for `tenders[]` generalization — DONE (2026-07-24)
- **File**: `src/shared/lib/printPayload.test.ts`
- **Satisfies**: fiscal-tender-code-mapping (all 3 requirements)
- Assert: 2 legs with distinct `printerCode`s ("05", "07") → 2 separate `pago05`/`pago07` lines, no `pago01`/`pago15` hardcode; 2 legs sharing the same `printerCode` → amounts ACCUMULATE into one line (not overwritten); a leg/gift-card with empty/falsy `printerCode` → `buildFacturaPayload` THROWS (no silent default code); regression — single-tender sale (today's shape) produces a byte-identical payload.
- **Result**: Rewrote the `buildFacturaPayload` describe blocks around the new `tenders[]` param (old `method`+`totalAmount`+`splitTender` positional signature removed the `computeGiftCardSplitTender` describe block entirely — that helper is superseded by tender construction at the `saleMachine.ts` call site, see 2.4's Result). Added: distinct-codes test, same-code-accumulates test, empty-code-throws test, gift-card-code-coexists-with-real-code test, multi-tender `montoigtf`-sums-all-legs test, and a byte-identical single-tender regression test. `buildNotaCreditoPayload`'s existing describe block needed NO signature changes (kept its own public `(method, totalAmount)` signature) — added one new test confirming its fixed `-999` → `GIFT_CARD_TENDER_CODE` mapping still works through the new internal `buildFacturaPayload` call.

### 2.4 [S] GREEN: printPayload.ts — implement `tenders[]`, real `printerCode`, accumulation — DONE (2026-07-24)
- **Depends on**: 2.3 (red), 1.2 (`printerCode` field)
- **File**: `src/shared/lib/printPayload.ts`
- Replace hardcoded `codeVal = method.id === -999 ? '15' : '01'` with a `tenders: { code: string; amountBs: number; igtfBs: number }[]` param built by the caller from `legs` (`method.printerCode`) + `giftCardLeg` (fixed `'15'`, unchanged).
- Accumulate by code (`Map<string, number>`) before formatting with `fixNumberForAPI`. Throw a clear error if any tender's code is falsy.
- Run `npm test` until green.
- **Result**: Implemented exactly as specified — `buildFacturaPayload(partnerName, partnerVat, lines, tenders, stationLabel)`; accumulates via `Map<string, number>`, throws `Error` if any `tender.code` is falsy, `montoigtf` is now the sum of every tender's `igtfBs` (was previously a single `calcIgtf(method, totalAmount)` call). `GIFT_CARD_TENDER_CODE` exported (was a private module const) so `saleMachine.ts` (the caller building `tenders[]`, per design.md's "built by the caller") can reuse it instead of re-hardcoding `'15'`. Removed `computeGiftCardSplitTender` (superseded — see Deviation note). Updated `buildNotaCreditoPayload`'s internal call site to pass a synthesized single-tender array (kept its historical hardcoded code, see Work Unit 2 Deviation #4). Updated `saleMachine.ts`'s `printFiscalInvoice` actor to build `tenders[]` from `legs`+`giftCard` (mapping `-999` legs to `GIFT_CARD_TENDER_CODE`, real legs to `method.printerCode`, and appending a separate gift-card tender only when the gift card isn't already represented as a `-999` leg, to avoid double-counting). `bun run test`: 20/20 new + 368/368 full suite green; `bun run typecheck`: clean.

---

## Phase 3 — Hook + UI wiring (depends on Phase 1 + Phase 2)

### 3.1 [P] RED: useVposCheckout — failing tests for `VPOS_LEG_PAID` dispatch + confirmed amount — DONE (2026-07-24)
- **File**: `src/features/payment/hooks/useVposCheckout.test.ts` (new)
- **Depends on**: 1.5 (event must exist)
- **Satisfies**: payment-flow / "VPOS Charge as Intermediate Leg", "Same VPOS Method Selectable" (dispatch shape only)
- Assert: on `codRespuesta === '00'`, dispatches `VPOS_LEG_PAID { payment, method, baseBs }` — NEVER `SUBMIT_PAYMENT` — where `baseBs` equals the CASHIER-CONFIRMED partial amount (new hook param), not blindly `remainingAmount`/`total`; navigates `/resultado` only when the confirmed amount covers the remainder, otherwise navigates back to `/pago` (loop, no close) without discarding prior legs.
- **Result**: Created the file (5 tests): dispatch-shape test asserting `event.type === 'VPOS_LEG_PAID'`/`!== 'SUBMIT_PAYMENT'` and `event.baseBs === confirmedBaseBs` (using `confirmedBaseBs=30` vs. `remainingAmount=100`/`totalWithIgtfBs=100` — deliberately different values, to prove `baseBs` isn't silently derived from either); a dedicated never-`SUBMIT_PAYMENT` regression; navigate-to-`/resultado` when `confirmedBaseBs >= remainingAmount`; navigate-to-`/pago` (loop) when `confirmedBaseBs < remainingAmount`, plus an assertion that only `VPOS_LEG_PAID` is ever sent (no `BACK`/`RESET` — the hook itself never discards legs, that's the machine's job); a `remainingAmount === null` regression (single VPOS-only sale, no gift card) falling back to `totalWithIgtfBs` to decide closing. Confirmed RED via `bun run test src/features/payment/hooks/useVposCheckout.test.ts`: 3/5 failed for the right reason (hook still dispatched `SUBMIT_PAYMENT` unconditionally and always navigated to `/resultado`; `confirmedBaseBs` param didn't exist yet) — the other 2 passed trivially because their asserted outcome (`/resultado`, single dispatch) coincided with the OLD unconditional behavior for those specific fixture values, not because the new contract was already implemented.

### 3.2 [S] GREEN: useVposCheckout.ts — generalize to `VPOS_LEG_PAID` — DONE (2026-07-24)
- **Depends on**: 3.1 (red)
- **File**: `src/features/payment/hooks/useVposCheckout.ts`
- Add `confirmedBaseBs: number` param (fed by 3.4). On `'00'`, `send({ type: 'VPOS_LEG_PAID', payment: {...}, method, baseBs: confirmedBaseBs })`; navigate based on `confirmedBaseBs >= (context.remainingAmount ?? totalWithIgtfBs)`.
- Run `npm test` until green.
- **Result**: Implemented exactly as specified — new required `confirmedBaseBs: number` param added to `UseVposCheckoutParams`; on `codRespuesta === '00'`, dispatches `VPOS_LEG_PAID { payment: {...}, method, baseBs: confirmedBaseBs }` (never `SUBMIT_PAYMENT`); navigates `confirmedBaseBs >= (context.remainingAmount ?? totalWithIgtfBs) ? '/resultado' : '/pago'`. Added `confirmedBaseBs`, `context.remainingAmount`, `totalWithIgtfBs` to the effect's dependency array (previously missing `totalWithIgtfBs` too — pre-existing staleness, harmless since `method` already changes on every leg, but tightened correctly here). `bun run test src/features/payment/hooks/useVposCheckout.test.ts`: 5/5 green. Full suite (`bun run test`): 44 files / 373 tests green; `bun run typecheck`: clean.
  - **Deviation (expected, flagged in the run instructions, not invented UI)**: `PaymentForm.tsx`'s only call site of `useVposCheckout` now needs a value for the new required `confirmedBaseBs` param, but the confirmation UI (`VposAmountInput`, tasks 3.3/3.4) doesn't exist yet. Passed a documented temporary default — `confirmedBaseBs: effectiveTotal` (i.e. `context.remainingAmount ?? total`, the same value already used for `amounts`/`totalWithIgtfBs`) — with a `TODO(generic-partial-payment, Work Unit 4 / tasks 3.3-3.4)` code comment at the call site. This preserves today's behavior exactly (one VPOS leg always closes the full remainder) until 3.3/3.4 wire a real cashier-confirmed value. No `VposAmountInput`/`PaymentForm.tsx` UI logic was added beyond this one-line param wiring, per scope instructions.

### 3.3 [P] RED: VposAmountInput — failing tests for pre-filled/editable-down amount — DONE (2026-07-24)
- **File**: `src/features/payment/components/VposAmountInput.tsx` + `.test.tsx` (new)
- **Depends on**: 0.2 (UX decision)
- **Satisfies**: post-design user decision (partial amount input)
- Assert: input mounts PRE-FILLED with `remainingAmount ?? total` (never empty); `max` attribute equals the remainder; typing/confirming a value ABOVE the remainder is rejected (clamped or confirm disabled); confirming without editing proceeds with the untouched full remainder (regression — preserves today's single-VPOS-close behavior); editing DOWN to a smaller valid value and confirming yields that value as the leg amount.
- **Result**: Created `VposAmountInput.test.tsx` (9 tests): pre-filled with `remainingAmount` when set, falls back to `total` when `remainingAmount` is `null` (regression), never mounts empty, `max` attribute equals the remainder, clamp-on-type-above-remainder (confirm never delivers more than the remainder), confirm-without-editing delivers the untouched remainder, edit-down-and-confirm delivers the edited value, confirm disabled on empty field, and `onBack`/cancel wiring. Confirmed RED via `bun run test`: import failed (`VposAmountInput.tsx` doesn't exist yet) — failed for the right reason.

### 3.4 [S] GREEN: VposAmountInput.tsx + PaymentForm.tsx wiring — DONE (2026-07-24)
- **Depends on**: 3.3 (red), 3.2 (hook param)
- **Files**: `src/features/payment/components/VposAmountInput.tsx`, `src/features/payment/pages/PaymentForm.tsx`
- Implement the input (default = remainder, `max` = remainder, no free/empty state). Gate `VposPaymentView`/`useVposCheckout`'s ping effect on a "confirmed" state set only after this input's confirm action; pass the confirmed value into 3.2's `confirmedBaseBs`.
- Run `npm test` until green.
- **Result**: Implemented `VposAmountInput` exactly per the RED spec — controlled `<input type="number">`, pre-filled with `remainingAmount ?? total`, `max`/clamp-on-change to the remainder, "Confirmar monto" button disabled while invalid/empty, "Cancelar y volver" delegates to `onBack`. Wired into `PaymentForm.tsx`: new `confirmedBaseBs` local state (`null` until confirmed); when `method.withMerchant` and not yet confirmed, renders `VposAmountInput` instead of `VposPaymentView`; `amounts` (`usePaymentAmounts`) now computes off the confirmed base for VPOS methods (`vposAmountBase = confirmedBaseBs ?? effectiveTotal`) instead of blindly off `effectiveTotal`, so the iframe/`totalWithIgtfBs` reflect the cashier's actual split choice once confirmed. Removed the temporary `confirmedBaseBs: effectiveTotal`/`TODO` from Work Unit 3. `bun run test`: full suite 46 files / 407 tests green (before final full-suite recount below); `bun run typecheck`: clean.
  - **Deviation #1 (necessary, not in the literal 3.4 file list, but required to satisfy the task's own instruction "gate the ping effect on a confirmed state")**: `useVposCheckout.ts` also needed a change. `PaymentForm.tsx` calls `useVposCheckout` unconditionally every render (React hook rule) regardless of which JSX branch is returned — so simply switching *what JSX renders* does NOT stop the hook's internal `useEffect` from pinging the VPOS terminal on mount. Added a new optional `confirmed?: boolean` param (**default `true`** — backward compatible: Work Unit 3's 5 existing tests call the hook without this param and keep pinging immediately, unaffected) that gates the effect: `if (!method?.withMerchant || !confirmed) return`. `PaymentForm.tsx` passes `confirmed={confirmedBaseBs !== null}`. Added 3 new RED→GREEN tests to `useVposCheckout.test.ts` for this specific gate (does not ping when `confirmed=false`; pings once `confirmed` becomes `true` on rerender; defaults to `true`/backward-compatible when omitted) — confirmed RED (2/3 failing for the right reason — terminal pinged despite `confirmed=false`) before implementing the gate. Also added 2 integration tests to `PaymentForm.test.tsx` (VposAmountInput shown before the iframe; iframe/VposPaymentView shown and ping starts after confirming) to lock in the end-to-end wiring.
  - **Deviation #2 (scope note, not a functional change)**: did not touch `usePaymentAmounts.ts` itself — the fix is entirely at the `PaymentForm.tsx` call site (which argument it passes), per the literal 3.4 file list.

### 3.5 [P] RED: PaymentSelect — failing tests for re-select, cap, printerCode filter, legs render — DONE (2026-07-24)
- **File**: `src/features/payment/pages/PaymentSelect.test.tsx`
- **Depends on**: 1.5 (`context.legs`), 1.2 (`printerCode`), 1.3 (`MAX_PAYMENT_LEGS`)
- **Satisfies**: payment-flow / "Same VPOS Method Selectable", "Method Availability Filtered by printer_code"; generic-partial-payment / "Leg Cap Enforcement"
- Assert: a method already used in a completed leg REMAINS selectable (no accidental de-dup); a method with empty/falsy `printerCode` is excluded/disabled with a message (gift-card synthetic method unaffected — fixed code, not filtered); when `(giftCardLeg ? 1 : 0) + legs.length >= MAX_PAYMENT_LEGS`, ALL selection is blocked with a clear message and existing `legs`/`remainingAmount` stay untouched; accumulated legs + remaining amount render when `legs.length > 0`.
- **Result**: Extended the file with 4 new describe blocks (9 new tests) alongside the pre-existing Scenario 4 block: re-selection of an already-used VPOS method (regression — no de-dup, already passed against the OLD code, confirming the "don't break this" constraint); `printerCode` filter (empty/falsy excluded, gift-card synthetic method unaffected, method with real code stays selectable); leg cap (blocks ALL selection incl. gift card at `tenderCount >= MAX_PAYMENT_LEGS` with a message, stays enabled below the cap, `legs`/`remainingAmount` untouched at the cap); legs/remainder render (`data-testid="legs-summary"` absent at `legs.length === 0`, present with the leg count + remainder text otherwise). `usePaymentMethods` mock changed from a static `{ data: [], isLoading: false }` to a mutable `mockMethods` variable so each test can control the method list. Confirmed RED via `bun run test`: 4/11 failed for the right reason (printerCode filter not implemented — empty-code method still rendered; cap not enforced — cards + message not blocked; legs summary not rendered) — the re-selection test passed trivially against the OLD code (expected, since it's a regression guard, not new behavior).

### 3.6 [S] GREEN: PaymentSelect.tsx — implement filter, cap, legs render — DONE (2026-07-24)
- **Depends on**: 3.5 (red)
- **File**: `src/features/payment/pages/PaymentSelect.tsx`
- Filter `methods` by `m.printerCode` truthy. Compute `tenderCount = (giftCardLeg?1:0) + legs.length`; disable/hide all cards + show "Máximo N medios de pago" when `tenderCount >= MAX_PAYMENT_LEGS`. Render legs/remainder summary. Do NOT add any de-dup-by-method-id logic (re-selection must keep working).
- Run `npm test` until green.
- **Result**: Implemented exactly as specified — `rawMethods.filter(m => !!m.printerCode)` (the synthetic gift-card method is built separately, outside this filter, so it's never affected); `tenderCount = (context.giftCardLeg ? 1 : 0) + (context.legs ?? []).length`; `capReached` hides the entire method grid (cards + gift card option) and shows "Máximo {MAX_PAYMENT_LEGS} medios de pago..." instead — no per-card disabling needed since nothing renders; `handleSelect` also early-returns on `capReached` as defense-in-depth (belt-and-suspenders, since no card is rendered to click in that state anyway); legs/remainder summary (`data-testid="legs-summary"`) renders only when `legs.length > 0`. Updated the stale Scenario-4 code comment ("solo se admiten exactamente 2 legs") to reflect the generalized N-leg/cap model. No de-dup-by-method-id logic was added (confirmed by the passing re-selection test). Added matching CSS (`.legsSummary`, `.capMessage`) to `PaymentSelect.module.css`. `bun run test`: 11/11 new + full suite green; `bun run typecheck`: clean.
  - **Deviation (test-infrastructure only, not a behavior change)**: the spec/task wording says empty-`printerCode` methods should be "excluded/disabled with a message" (an explicit OR). Implemented pure exclusion (not rendered at all) — the spec's own scenario text for this requirement ("Método sin printer_code hidden from selection") only requires the method to not appear, no per-card message is spec-mandated, and pure exclusion is simpler/lower-risk than adding disabled-card UI with no test demanding it.

---

## Phase 4 — Integration / regression / manual gate (sequential, last)

### 4.1 [S] Retry/resiliencia integration test — VPOS timeout/rejection mid multi-leg sale — DONE (2026-07-24)
- **Depends on**: 1.5, 3.2
- **Satisfies**: generic-partial-payment / "VPOS Leg Failure Recovery Preserves State"
- Add an integration test to `saleMachine.test.ts` (analogous to the prior change's offline-replay integration test): drive 2 completed VPOS legs, then a 3rd VPOS attempt that receives `codRespuesta !== '00'` (no `VPOS_LEG_PAID` dispatched). Assert `context.legs` (2 entries) and `context.remainingAmount` are BYTE-IDENTICAL to their pre-attempt values, the machine stays out of `processing`/`paymentError`, and the cashier can retry the same or a different method without re-entering prior legs.
- **Result**: Found this test ALREADY WRITTEN and ALREADY GREEN in `src/features/payment/machines/saleMachine.test.ts` (describe block "saleMachine — generic N-leg VPOS payment", test titled `T4.1 retry/resiliencia: ...`, lines ~754-839) — it was part of the 45-file/401-test baseline confirmed green at the start of this work unit, i.e. written in an earlier, undocumented pass at this same task (tasks.md was never updated to reflect it). **No RED phase was executed by me for this task** — there was no production code to write; the test only proves that NOT dispatching an event leaves `context` untouched, which was already an inherent property of the machine since Work Unit 1 (1.4/1.5). Rather than fabricate a RED step retroactively, I independently re-verified the test is a REAL, non-trivial integration test (not a smoke test): it drives the actor through 2 real `VPOS_LEG_PAID` commits (gift card $30 + VPOS A $30 + VPOS B $15, `remainingAmount` 100→70→40→25), snapshots `legs`/`remainingAmount` before the "failed" 3rd attempt, asserts byte-identity after (no event sent — this correctly reproduces the REAL contract, since I cross-checked `useVposCheckout.ts:94-97`: on `codRespuesta !== '00'` the hook only calls `pushToast('error', ...)`, it NEVER dispatches `VPOS_LEG_PAID` or any other event to the machine), then exercises the actual retry path (`BACK` → `selectingMethod`, re-`SELECT_METHOD` with a different method, successful `VPOS_LEG_PAID` closing the sale at `legs.length === 3`, `remainingAmount === 0`). Confirmed GREEN by running `bun run test src/features/payment/machines/saleMachine.test.ts` (33/33 passing) and the full suite (45 files/401 tests, unchanged). No implementation code was touched — none was needed.
  - **TDD Mode deviation, explicitly flagged**: Strict TDD requires RED→GREEN driven by me in this session. That did not happen here because the test pre-existed from an earlier, unrecorded session. This is reported transparently rather than silently claimed as a fresh TDD cycle — see the TDD Cycle Evidence table in the apply-progress artifact/return summary.

### 4.2 [S] Full regression pass — gift-card + 1 VPOS method (existing shipped case) — DONE (2026-07-24)
- **Depends on**: all of Phase 1-3
- **Satisfies**: generic-partial-payment / "Regression — Existing Single/Two-Leg Flows Unaffected"
- Run the full `npm test` suite + `npm run typecheck`. Confirm the gift-card + 1-VPOS-method sale still produces exactly 2 tender legs summing to total, BYTE-IDENTICAL to pre-change output, with ONE explicitly documented, conscious exception: `montoIgtf`/`montoigtf` are now computed via real `calcIgtf(method, baseBs)` instead of a forced `0` (per 2.1/2.2's documented change, requirement #9) — numeric result is unchanged today since no production method has `applyIgtf: true`.
- **Result**: `bun run test` → **45 files / 401 tests, all green**; `bun run typecheck` → clean (no errors). Gift-card + 1-VPOS regression confirmed via existing tests, still passing unchanged: `saleMachine.test.ts` "printing wiring: the REAL printFiscalInvoice ... splits the fiscal invoice into two tenders (pago15 + pago01) for the 2-leg remainder" and "T4.1 offline replay integration" (both exercise the real, non-mocked builders end-to-end); `useGiftCardPayment.test.ts` "balance >= total: dispatches a FULL SUBMIT_PAYMENT ... (regression, Scenario 1)" for the full-balance-closes-immediately case. `montoIgtf`/`montoigtf` exception confirmed unchanged from Work Unit 2 (still `calcIgtf(method, baseBs)`, numerically 0 today).
  - **Default-behavior regression re-confirmed for the user's new "Saldo a consumir" input**: "consume full card balance by default, without touching the input" is the passing, documented default — `useGiftCardPayment.test.ts` line ~52 (`consumedAmountUSD === orderTotalUSD` when `balance >= total` and the input is never edited) and line ~127 (`SUBMIT_PAYMENT` dispatched, no `GIFT_CARD_PARTIAL`, no navigation to `/pago`) are both green and untouched by the new input's addition — `handleSearchCard` pre-fills `consumedAmountInput` with `Math.min(balance, orderTotalUSD)` on card lookup, so "don't touch the input" reproduces exactly today's shipped behavior.
  - **New case introduced by the input, NOT covered by the original spec.md — flagged, not implemented (out of scope for this task)**: the input lets the cashier VOLUNTARILY consume LESS than `orderTotalUSD` even when `balance >= total` (e.g. balance=10, total requires 2.9, cashier types "1.0"), which now dispatches `GIFT_CARD_PARTIAL` and forces a second leg — a scenario the original `gift-card-partial-payment` spec never describes, because that spec's branching is balance-driven (`balance >= total` ⇒ always full/immediate close), not cashier-choice-driven. This is already implemented (by the user, not by this task) and already tested green (`useGiftCardPayment.test.ts` "dispatches GIFT_CARD_PARTIAL when custom consumed amount is lower than orderTotalUSD"), so functionally it works — but `specs/spec.md`'s "Full balance covers total (regression, unchanged)" scenario text is now stale/incomplete: it should be amended (in a future spec revision, not here) to state the full-close-and-block-second-leg behavior only applies when the cashier does NOT edit the amount down. Not fixed here — flagged per explicit scope instruction.
  - **Secondary, minor edge case flagged (not a bug, not fixed)**: with the new `step="0.0001"` precision, `remainingBs = total - consumedAmountUSD * globalRate` can in principle land on a tiny non-zero float (e.g. `0.004`) instead of exactly `0` due to floating-point arithmetic on a 4-decimal USD input multiplied by an integer/float `globalRate`. This would make the machine treat the sale as still having a (negligible) remainder, requiring one more VPOS leg for a fraction of a bolívar. This risk pre-dates this change at the 2-decimal precision level too (not newly introduced, only made marginally more likely by finer-grained input) and is not exercised by any current test with adversarial float inputs. No fix applied — outside the 4.1/4.2 scope and not a regression from this change.

### 4.3 [S] Hardware validation gate — 3+ tenders on the real fiscal printer (PENDING, manual, not automatable)
- **Status: PENDING.** Not executed in this environment (no physical ESC/POS fiscal printer available). Left unmarked/not DONE, per explicit scope — this is a release gate for a human before production, not an automatable apply task.
- **Depends on**: 2.4, 3.6
- **Satisfies**: fiscal-tender-code-mapping (physical confirmation), design Migration/Rollout release gate
- Cannot be executed in this environment (no physical ESC/POS fiscal printer). Checklist for a human before production release:
  1. Configure a real kiosk station against an actual fiscal printer via `printer-agent` (not the mocked `FiscalPrinterAdapter`).
  2. Run a real sale with gift card + 2 sequential VPOS legs (3 tenders total, at least one pair sharing a `printerCode` to exercise accumulation).
  3. Physically inspect the printed invoice: confirm 3 distinct/accumulated tender lines print correctly and sum exactly to the order total (no missing/duplicated/garbled line).
  4. Confirm the printer does not silently collapse multiple `pago<code>` fields or misinterpret an unfamiliar code per the vendor's undocumented `ServWebImpresion` tender table.
  5. Only after physical confirmation is this release gate closed. If codes are wrong, the fix is confined to the Odoo `x_pos_payment_method.printer_code` data (no code change needed).

---

## Task-to-Requirement Traceability

| Capability / Requirement | Tasks |
|---|---|
| generic-partial-payment / N-Leg Payment Composition | 1.4/1.5, 2.1/2.2, 3.1/3.2, 4.2 |
| generic-partial-payment / Leg Cap Enforcement | 3.5/3.6 |
| generic-partial-payment / VPOS Leg Failure Recovery | 1.4/1.5, 4.1 |
| generic-partial-payment / IGTF Calculated Per Leg | 2.1/2.2, 4.2 |
| generic-partial-payment / Regression Single/Two-Leg | 4.2 |
| fiscal-tender-code-mapping / Real printer_code Sourced | 1.1/1.2, 2.3/2.4 |
| fiscal-tender-code-mapping / Defensive Accumulation | 2.3/2.4 |
| fiscal-tender-code-mapping / Empty printer_code Blocks | 2.3/2.4, 3.5/3.6 |
| gift-card-partial-payment / Optional Leg With Remainder (existing) | 1.5 (legacy fallback), 4.2 |
| gift-card-partial-payment / Multi-Leg Payload & Fiscal Print | 2.2, 2.4 |
| payment-flow / VPOS Charge as Intermediate Leg | 1.4/1.5, 3.1/3.2 |
| payment-flow / Same VPOS Method Selectable | 3.5/3.6 |
| payment-flow / Method Availability Filtered by printer_code | 3.5/3.6 |
| Post-design decision: prefilled/editable-down VPOS amount input | 0.2, 3.3/3.4 |

---

## Parallelization Summary
- **Phase 0**: sequential, decision-only, no code — unblocks everything else.
- **Phase 1**: 1.1→1.2 and 1.4→1.5 are independent pairs; 1.3 is fully independent (any time).
- **Phase 2**: 2.1→2.2 and 2.3→2.4 run in parallel once 1.2/1.5 land.
- **Phase 3**: 3.1→3.2, 3.3→3.4, 3.5→3.6 run in parallel once their Phase 1/2 dependencies land.
- **Phase 4**: strictly sequential, last, gates release (4.3 is a manual, non-automatable gate).
